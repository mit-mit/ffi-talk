name: sqlite
description: An iOS app that shows the fruits and veggies currently in season.

version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  ffi: ^1.0.0

dev_dependencies:
  ffigen: ^2.3.0
